// 담당: 이유민
// 하교 흐름 (3단계): 색 선택 → AI 고정 질문(녹음) → 아이템 생성 + 섬 배치
// 참고: docs/planning/PLANNING.md "[학생 화면] — 하교 흐름"
// ColorPicker, ChatPanel, ItemReveal은 checkin과 동일 컴포넌트 재사용 (props로 flow="checkout" 구분 권장)

export default function CheckoutPage() {
  return (
    <main>
      {/* TODO(이유민): 색 선택 + 고정 질문 대화 + 아이템 생성 조립 */}
      {/* TODO: 완료 후 <IslandBoard /> (강윤지) 로 이동 */}
    </main>
  );
}
