// 담당: 이유민
// 등/하교 4단계: 대화에서 추출한 아이템 1개 미리보기 + "섬에 배치하러 가기" 버튼
// 참고: docs/prototype/prototype-student.html #s4

export default function ItemReveal({
  emoji,
  name,
  reason,
}: {
  emoji?: string;
  name?: string;
  reason?: string;
}) {
  return <section>{/* TODO(이유민): 아이템 카드 + CTA 버튼 */}</section>;
}
