// 담당: 이지현 (단독 소유)
// 협진 챗봇 아이콘. 로봇 이모지(🤖)가 "살핌"의 따뜻한 톤과 안 맞는다는 피드백으로 교체.
// 올빼미 = "지혜로운 조언자" 상징(듀오링고 등에서도 흔히 쓰는 은유) — 챗봇이되 차갑지 않게.
// 눈동자 색은 앱 전체에서 쓰는 인디고(#6366f1)를 그대로 써서 브랜드 색과 자연스럽게 이어지게 함.
//
// (2026-09-19) "올빼미만 보면 챗봇인지 바로 안 와닿는다"는 피드백 — 헤드셋을 씌워서 "상담
// 도우미"라는 신호를 더했다. 색은 새로 안 늘리고 눈동자와 같은 인디고를 그대로 써서 통일감 유지.
//
// (2026-09-19 수정 1) 처음엔 헤드셋을 몸통보다 먼저 그려서 좌표가 전부 몸통 원 안에 들어가
// 있었다 — 불투명한 몸통이 그 위를 덮어 헤드셋이 통째로 안 보였다("올빼미가 안 바뀌었는데?").
// 순서를 몸통 → 헤드셋 → 눈으로 바꿔서 눈에 보이게는 고쳤다.
//
// (2026-09-19 수정 2) 그런데도 "헤드셋 같지도 않다"는 피드백 — 가느다란 선 하나로만 그려서
// 헤드폰이라기보다 그냥 구불구불한 선으로 보였다. 실제 이미지 생성 도구가 없어서, 대신
// 실제 헤드셋처럼 보이도록 벡터를 다시 그렸다: 얇은 스트로크 대신 두꺼운 헤드밴드(strokeWidth 3.4)
// + 머리 양옆으로 튀어나온 진짜 이어컵(패드 하이라이트가 있는 타원 두 겹) + 마이크 붐대. 이어컵이
// 머리 밖으로 나가야 해서 viewBox를 "0 0 32 32"에서 "-4 -6 40 40"으로 넓혔다(머리 크기는 그대로,
// 여백만 늘림). sharp로 PNG 렌더링해서 실제로 헤드셋처럼 보이는지 확인한 뒤 반영.
export default function AgentOwlIcon({ className }: { className?: string }) {
  return (
    <svg viewBox="-4 -6 40 40" className={className} aria-hidden="true">
      {/* 몸통 */}
      <circle cx="16" cy="18" r="13" fill="#EFCB9C" />
      {/* 귀깃 */}
      <path d="M8 9 L12 6 L11 12 Z" fill="#D9A86C" />
      <path d="M24 9 L20 6 L21 12 Z" fill="#D9A86C" />
      {/* 헤드셋 밴드 — 머리 위로 두껍게 걸침 */}
      <path d="M2 14 Q16 -3 30 14" fill="none" stroke="#6366f1" strokeWidth="3.4" strokeLinecap="round" />
      {/* 이어컵 — 머리 양옆으로 튀어나온 타원 + 패드 하이라이트 */}
      <ellipse cx="1.5" cy="18" rx="3.6" ry="4.8" fill="#6366f1" />
      <ellipse cx="1.5" cy="18" rx="2" ry="3" fill="#c7d2fe" />
      <ellipse cx="30.5" cy="18" rx="3.6" ry="4.8" fill="#6366f1" />
      <ellipse cx="30.5" cy="18" rx="2" ry="3" fill="#c7d2fe" />
      {/* 마이크 붐대 */}
      <path d="M29.5 21 Q29 28 19 24" fill="none" stroke="#6366f1" strokeWidth="1.6" strokeLinecap="round" />
      <circle cx="19" cy="24" r="1.3" fill="#6366f1" />
      {/* 눈 */}
      <circle cx="11.2" cy="16.5" r="5.6" fill="#fff" />
      <circle cx="20.8" cy="16.5" r="5.6" fill="#fff" />
      <circle cx="11.9" cy="16.8" r="2.8" fill="#6366f1" />
      <circle cx="20.1" cy="16.8" r="2.8" fill="#6366f1" />
      <circle cx="12.7" cy="15.9" r="0.9" fill="#fff" />
      <circle cx="20.9" cy="15.9" r="0.9" fill="#fff" />
      {/* 부리 */}
      <path d="M14.3 22.5 L17.7 22.5 L16 25.6 Z" fill="#E8963C" />
    </svg>
  );
}
